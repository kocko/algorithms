#include <bits/stdc++.h>

#define endl '\n'

using namespace std;
typedef long long hyper;

int main(void) {
  int x;
  while (cin >> x) {
    if (x == 42) break;
    cout << x << endl;
  }
  return 0;
}